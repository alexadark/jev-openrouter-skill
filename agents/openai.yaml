interface:
  display_name: "Jev via OpenRouter"
  short_description: "Structured Jev decisions and model advice"
  default_prompt: "Use $jev-openrouter to recommend a model and reasoning effort for this task without starting the task."
policy:
  allow_implicit_invocation: false
